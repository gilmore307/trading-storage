# Tasks

This is the active storage task ledger. Keep lifecycle and dashboard work dry-run-first unless a reviewed slice explicitly authorizes a narrower mutation.

## Active Tasks

- Finish the next storage lifecycle phase in controlled slices. Implemented pieces already cover file-backed SQL archive copies, archive restore verification, no-mutation quarantine/delete receipts, maintenance inventory and lifecycle gap audit, compact-contract lifecycle gap actions, artifact indexing, protected-set evidence, quarantine/recheck evidence, compression/archive/restore scaffolds, narrow single-file compression, file-backed archive copies, latest-only dashboard read models, and one-pass safe file lifecycle acceptance.
- Keep broad/destructive lifecycle execution unauthorized. Current allowed writes are compressed-copy creation for unprotected `compress_candidate` regular files, gzip archive-copy creation for unprotected file-backed `archive_candidate` records, reviewed bounded dashboard read-model pruning, and explicit `run_storage_maintenance.py --apply-lifecycle-gap-actions` handling for listed replay, post-replay, TE recent-refresh, and realtime monitor classes after compact contracts are written.
- Preserve the no-broker/no-provider boundary: storage helpers must not call providers, activate models, execute broker actions, mutate accounts, detach/drop SQL, move physical quarantine payloads, physically delete protected source/model evidence outside reviewed lifecycle classes, or install/enable host timers without review.
- Maintain storage-hosted dashboard read models for the current public set: `current_system_status_summary`, `historical_task_progress_summary`, `temporal_explorer_summary`, `realtime_signal_summary`, `execution_realtime_trading_runtime_status`, `model_readiness_summary`, and `model_promotion_posture_summary`.

## Current Accepted Surfaces

- Local receipt payload persistence is implemented through `src/trading_storage/artifact_store.py` and `scripts/artifacts/store_completion_receipt_payload.py`.
- Local lifecycle maintenance is implemented through `src/trading_storage/lifecycle.py` and `scripts/lifecycle/maintain_local_storage.py`. The accepted trading runtime route is `storage/90_lifecycle/{tmp,cache,staging,logs,runs,outputs}`; repository-local runtime roots are covered by the same cleanup policy when present.
- Storage maintenance is implemented through `src/trading_storage/storage_maintenance.py`, `scripts/lifecycle/run_storage_maintenance.py`, and deployable systemd templates under `deploy/systemd/`. The default pass emits root inventory, lifecycle gap findings, and report-only action summaries. Explicit `--apply-lifecycle-gap-actions` writes compact contracts and applies the listed state-triggered delete/compress/rolling actions; checked-in service/timer files are templates only and are not installed by repository changes.
- Dashboard read-model materialization is implemented through `src/trading_storage/dashboard_read_models.py` and `scripts/dashboard/materialize_read_model.py`.
- Public dashboard refresh orchestration is implemented through `scripts/dashboard/refresh_public_dashboard_read_models.py` plus the contract-specific refresh helpers.
- Lifecycle policy and receipts are documented in `docs/20_storage_lifecycle_policy.md` and `docs/21_lifecycle_receipts.md`.
- Per-file-class storage cleanup decisions are documented in `docs/22_storage_maintenance_playbook.md`.
- Artifact index, protected-set, compression/archive, and dashboard read-model details are owned by `docs/30_artifact_index.md`, `docs/31_protected_set.md`, `docs/32_compression_archive.md`, `docs/40_dashboard_read_models.md`, and `docs/41_dashboard_summary_layout.md`.

## Storage Maintenance Review Queue

These are reviewed maintenance candidates, not deletion authorization. Each cleanup slice still needs artifact-index classification, protected-set evidence, quarantine or recheck evidence, and a receipt unless the item is explicitly a latest-only dashboard cache.

| Priority | Path or file class | Current evidence | Required maintenance action | Status |
| --- | --- | --- | --- | --- |
| P0 | `storage/06_dashboard_cache/**/snapshots/` | Timestamped dashboard snapshots previously grew without bound; cache is now one-file-per-contract and about 296K with zero timestamped snapshots. | Keep `read_models/<contract>.json` and schema only. Scheduled refresh should continue auto-pruning timestamped read-model snapshots. | Verified 2026-06-06: dry-run found zero snapshot records, index had zero rows, empty snapshot directories were removed. Monitor for regression. |
| P0 | `storage/01_source_data/monthly_backfill/alpaca_bars/<symbol>/<YYYY-MM>/` | Operator confirmed bars are retained in SQL. Raw duplicate payload files were 10,724 `equity_bar.jsonl`/`equity_bar.csv` files, about 12.30 GiB. | Keep compact provenance files only: request manifests, schemas, completion receipts, and task keys. Do not regenerate JSONL/CSV payload copies unless SQL rebuild/debug explicitly requires them. | Completed 2026-06-06: deleted JSONL/CSV payload files directly; directory reduced from 13G to 170M; remaining payload-file count is 0. |
| P0 | `storage/05_replay_datasets/promotion_replay_candidate_policy/replay_execution_runs` and `post_replay_attribution_runs` | Audit on 2026-06-13 found `05_replay_datasets` about 7.1G: replay execution runs about 2.6G including one 2.3G `decision_rows.jsonl`, and post-replay attribution runs about 4.1G with repeated 124.6M `event_interpretations.jsonl` files. | Add replay completion-triggered compact run manifests, attribution summaries, and exception rules for unresolved/promotion-linked evidence. Final handling should be delete for rebuildable verbose rows or rolling retention for repeated completed runs. | Completed 2026-06-13: `--apply-lifecycle-gap-actions` wrote compact manifests, kept recent full runs, deleted/rolled verbose replay and attribution rows, and reduced promotion replay policy storage to about 842M. |
| P0 | `storage/02_control_plane/runtime/model_05_option_expression/**/task_key.json` and `storage/02_control_plane/runtime/provider_task_keys/**/task_key.json` | Audit on 2026-06-13 found 13,480 `task_key.json` files across storage, with M05/provider task keys still present. These are generated control-plane metadata, not durable market source data. | Replace per-request task-key sprawl with compact batch manifests plus accepted receipts. Delete old per-request keys only after provider tasks complete, compact manifests exist, failure-register disposition is clear, and retry receipts remain available. | Compact aggregate manifests implemented; deletion remains blocked because existing task keys lack explicit terminal status fields. |
| P0 | `storage/02_control_plane/runtime/model_05_option_expression/gate_review/*.json` | Earlier evidence said one 2016-01 gate review was about 103M, but the 2026-06-13 filesystem audit did not find that large gate-review file. Gate decisions remain important evidence but this item needs evidence refresh before mutation work. | Preserve compact gate decision, blocker, repair, retry, and receipt references if verbose gate dumps recur. Compress/delete verbose diagnostic arrays only after a compact gate-review contract exists. | Reclassify as stale audit item; verify removed or relocated before implementation. |
| P1 | `storage/04_execution_artifacts/runtime/realtime_monitor/<timestamp>/` | Audit on 2026-06-13 found 8,178 timestamped directories and about 129M by disk usage. | Keep latest/current-window monitor state plus compact time-series summary. Apply rolling retention to old `loop_receipt.json` and `cycle_*.json` directories after dashboard summaries and alert evidence are retained. | Completed for normal loops 2026-06-13: rolling summary implemented, recent/exception loops retained, old normal completed timestamp dirs removed; root now about 1.9M. |
| P1 | `storage/02_control_plane/runtime/historical_scheduler_decisions.jsonl` | Dashboard status uses it as a throughput source; JSONL logs will grow indefinitely. | Add log rollup and compact scheduler-decision summary. Dashboard should read the compact summary for ordinary status. | Compact rollup implemented; truncation remains blocked until segmented tail handling exists. |
| P1 | `storage/02_control_plane/runtime/stage_coverage/` and `storage/02_control_plane/runtime/stage_run_dashboard/` | Derived control-plane/dashboard views; not primary source data. | Keep latest/current summary and bounded historical aggregates, not unlimited detailed snapshots. | Compact rollups implemented; detailed snapshot deletion remains blocked until latest pointer/aggregate consumers are verified. |
| P1 | `storage/01_source_data/monthly_backfill/trading_economics_calendar_web/YYYY-MM/runs/{calendar_maintenance_*_te,te_recent_calendar_refresh_*}/` | TE source rows/payloads are protected and canonical under monthly backfill. Audit on 2026-06-13 found 655 `calendar_maintenance_*_te` run directories and about 837M across 2026-06/2026-07. Calendar maintenance now runs TE discovery daily, writes only new or changed source facts, cleans up no-op refreshes without receipts/byproducts, schedules one-shot release jobs at discovered future release times, polls TE every 5 seconds for up to 60 seconds for formal `actual` values, and writes provisional web-search fallback evidence only when TE is late. TE side products are not protected data once compact provenance/reconciliation exists. | Preserve TE source rows/payloads. Use lifecycle selectors for duplicate receipts/manifests, diagnostics, no-op context, and provisional fallback artifacts after formal TE capture. Compact repeated run evidence into concise month-level provenance; do not delete TE source rows/payloads. | Monthly run side-product rolling implemented 2026-06-13: compact TE source provenance is written, recent run-local receipts/manifests are retained, and older `completion_receipt.json` / `request_manifest.json` files roll forward without touching `saved/` or `cleaned/` payloads. |
| P1 | `storage/01_source_data/monthly_backfill/trading_economics_calendar_web/_manifests/recent_refresh_runs` and `source_consolidation_*` | Audit on 2026-06-13 found `_manifests` about 556M, mostly `recent_refresh_runs` about 495M of repeated completion receipts; cold source-consolidation evidence is about 59M. | Preserve TE source rows/payloads. Compact repeated refresh receipts to month-level provenance and use rolling retention for run-local byproducts. Treat consolidation evidence as cold evidence and compress/archive bulky working copies after concise proof exists. | Recent refresh rolling completed 2026-06-13: compact provenance written and recent receipts retained; recent refresh manifests now about 25M. Source-consolidation compression remains pending. |
| P2 | `storage/02_control_plane/runtime/model_02_target_state/input_materialization/` | About 77M. Contains intermediate `bars.jsonl` copies when source and SQL lineage should already identify the input window. | Classify as intermediate materialization/debug sidecar. Delete or compress after fold closure unless referenced by model/dataset lineage. | Gap audit selector added; lineage check remains pending. |
| P2 | `storage/01_source_data/model_05_option_expression/` | Current size is modest, about 85M, but future full option-chain ingestion can dominate storage. | Store shared option source once for M02 and M05 reuse. Keep SQL/canonical source plus compact provenance; avoid model-local duplicate payload copies. | Coordinate with option-source contract work. |

## Maintenance System Improvements

- Extend maintenance beyond root inventory and fold-scoped cleanup. It now emits known lifecycle gap classes, writes compact contracts, and applies explicit state-triggered gap actions for selected P0/P1 classes. Next slices should add terminal status to task keys, segment scheduler JSONL tails, verify stage latest pointers, and handle TE source-consolidation cold compression.
- Add explicit retention metadata from producers for generated task keys, gate reviews, scheduler decisions, stage coverage views, realtime monitor cycles, and intermediate materialization files.
- Keep source-data exceptions narrow. Trading Economics canonical source remains protected; derived TE runtime/dashboard/control-plane copies should be empty, compact, or explicitly classified as derived.
- Do not let receipts, manifests, dashboards, or task keys become larger than the data they help operate. For each such class, retain one current summary plus bounded evidence needed for audit, retry, or restore.

## Not Current Historical-Training Scope

These items are outside the current no-broker historical-training run and must not be treated as active storage work items:

- production object-store backend selection;
- production lifecycle mutation without artifact index, protected-set, quarantine/recheck, receipt, and reviewed executor coverage;
- development-to-durable promotion automation before a concrete consumer requires it;
- production queue execution and storage-resident lifecycle mutation;
- semantic dashboard summary producers beyond the current public set, dashboard read adapters, or lifecycle timers before a controlled implementation slice is accepted;
- broad movement of every existing local development artifact into storage before concrete paths and acceptance gates exist;
- host-level timer enablement without operator review.
