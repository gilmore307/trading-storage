# Tasks

## Active Tasks

- Implement the next storage lifecycle phase in controlled slices. The reviewed file-backed SQL archive executor, archive restore verifier, no-mutation quarantine/delete receipt builder, and scheduled maintenance wrapper are now implemented. Remaining lifecycle runtime work is limited to fold SQL-backup consumption and separately reviewed destructive mutation executors.
- Keep broad/destructive lifecycle execution dry-run-only until mutation executors and receipts are implemented and reviewed. The V0.1 filesystem artifact index, protected-set builder, durable-artifact lifecycle planner, quarantine/recheck evidence builder, compression/archive/restore execution scaffold, narrow single-file compression executor, reviewed file-backed SQL archive executor, archive restore verifier, no-mutation quarantine/delete receipt builder, dashboard snapshot prune helper, and one-pass safe file-lifecycle acceptance helper are implemented. Current allowed writes are compressed-copy creation for unprotected `compress_candidate` regular files and gzip archive-copy creation for unprotected file-backed `archive_candidate` records. Dashboard snapshot deletion is temporarily on hold until event-risk-governor regeneration and downstream review close; original source deletion, Layer 1/2 data deletion, artifact-index mutation, SQL detach/drop, physical quarantine moves, physical deletion, and daemon execution remain unauthorized.

The initial storage-contract-and-lifecycle-helper phase is closed. Current historical training can use local receipt payload persistence, artifact refs, manager summary rows, and dry-run-first local lifecycle maintenance. The new V0.1 lifecycle policy is accepted as the next shape but does not authorize production deletion, SQL detach/drop, or daemon execution yet.

## Historical-Training Todo Status

- Local generated artifacts remain ignored and storage-owned.
- Durable/system-owned non-SQL saved data is now accepted as storage-owned by default. Trading runtime disposable cache/tmp/local staging should also live under storage-owned ignored roots and be cleaned by storage-owned scheduled maintenance; component-local saved files are transitional only unless a narrower exception is accepted.
- Completion receipt payload storage is implemented through `src/trading_storage/artifact_store.py` and `scripts/artifacts/store_completion_receipt_payload.py`.
- Local lifecycle maintenance is implemented through `src/trading_storage/lifecycle.py` and `scripts/lifecycle/maintain_local_storage.py`; it now covers both legacy component-local roots and target storage-owned `storage/90_lifecycle/tmp`, `storage/90_lifecycle/cache`, `storage/90_lifecycle/staging`, `storage/90_lifecycle/logs`, `storage/90_lifecycle/runs`, and `storage/90_lifecycle/outputs` roots.
- Scheduled storage maintenance is implemented through `src/trading_storage/storage_maintenance.py`, `scripts/lifecycle/run_storage_maintenance.py`, and deployable systemd templates under `deploy/systemd/`. The current scheduled phase inventories all numbered storage roots, applies local retention, and reads manager fold state files directly for completed ten-layer model-worker folds; completed folds become storage-owned SQL backup candidates and may expose `storage/01_source_data/fold_scoped/<fold_id>/` source cleanup candidates without a manager-authored backup/cleanup signal, request, or plan.
- Dashboard read-model materialization is implemented through `src/trading_storage/dashboard_read_models.py` and `scripts/dashboard/materialize_read_model.py`: producer-supplied summary payloads are validated and written to storage-owned snapshot/latest/schema/index paths under `storage/06_dashboard_cache/`.
- Dashboard read-model refresh orchestration is implemented for `historical_task_progress_summary` through `src/trading_storage/dashboard_refresh.py`, `scripts/dashboard/refresh_historical_task_progress_read_model.py`, and reviewed systemd service/timer templates. The refresh runs the manager-owned semantic producer, validates/materializes output, and performs no provider calls, model activation, broker execution, or account mutation.
- Maintenance and dashboard refresh systemd templates are checked in but intentionally not installed or enabled.
- V0.1 lifecycle design is documented in `docs/20_storage_lifecycle_policy.md` through `docs/21_lifecycle_receipts.md`: Layer 1/2 data are persistent by default, later-layer run metadata may expire after model-run cycle close, promoted model bodies are kept permanently, source data is compressed before deletion unless disposable, SQL detail is archived through export/restore workflows, all lifecycle actions require manifest/receipt evidence, promotion may classify retention intent but not execute cleanup, and broad requested lifecycle work remains visible through manager task summaries. Fold-completion backup/delete is the narrower storage-scheduled path: storage reads manager fold runtime state directly and does not wait for a manager signal/request/plan. Reusable Layer 1/2 source foundations are not deletion candidates; explicitly fold-scoped target/source folders may become cleanup candidates only as `storage/01_source_data/fold_scoped/<fold_id>/` after the full fold closes. `docs/20_storage_lifecycle_policy.md` now has the first durable-artifact dry-run planner, quarantine/recheck evidence builder, compression/archive/restore execution scaffold, narrow single-file compression executor, and one-pass safe file-lifecycle acceptance helper. `docs/30_artifact_index.md` has the first filesystem JSONL artifact-index builder for dry-run inventory. `docs/31_protected_set.md` has the first conservative protected-set builder and report-only quarantine/recheck gate for safety evidence. `docs/32_compression_archive.md` now has draft manifest/receipt scaffold behavior and single-file compressed-copy execution for compression candidates. `docs/40_dashboard_read_models.md` records that dashboard summary/read-model outputs belong in storage and now has a bounded snapshot-prune helper for high-frequency metadata caches; `docs/41_dashboard_summary_layout.md` defines and now has the first helper for the accepted physical JSON layout and common validation boundary.

## Not Current Historical-Training Scope

These items are intentionally outside the current no-broker historical-training run and must not be treated as active storage work items:

- production object-store backend selection;
- production lifecycle mutation before artifact index/protected-set/receipt support exists;
- development-to-durable promotion automation before a concrete consumer requires it;
- production queue execution and storage-resident lifecycle mutation;
- additional semantic dashboard summary producers, dashboard read adapters, or lifecycle timers before a controlled implementation slice is accepted;
- broad migration of every existing local development artifact into storage before artifact index/protected-set/read-model/staging-root implementation slices define concrete paths and acceptance gates;
- host-level timer enablement without operator review.

## Recently Accepted

- Implemented reviewed file-backed SQL archive and restore verification scripts: `src/trading_storage/sql_archive.py`, `scripts/lifecycle/execute_sql_archive.py`, and `scripts/lifecycle/verify_sql_archive_restore.py` can plan or write gzip archive copies for unprotected `archive_candidate` records and verify archive checksums without database connections, SQL detach/drop, source deletion, quarantine moves, artifact-index mutation, model activation, broker execution, or account mutation.
- Implemented no-mutation quarantine/delete gate receipts: `src/trading_storage/quarantine_delete_executor.py` and `scripts/lifecycle/build_quarantine_delete_result.py` consume quarantine/recheck evidence and emit quarantine/deletion/tombstone draft receipts while keeping physical quarantine moves and deletion disabled.
- Implemented one-pass safe file lifecycle acceptance: `src/trading_storage/file_lifecycle_acceptance.py` and `scripts/lifecycle/run_file_lifecycle_acceptance.py` run artifact index, protected set, dry-run lifecycle plan, quarantine/recheck evidence, execution scaffold, optional compressed-copy creation for unprotected `compress_candidate` files, and dashboard snapshot prune dry-run while preserving originals and avoiding deletion/SQL/quarantine/index mutations.
- Implemented dashboard snapshot metadata pruning: `src/trading_storage/dashboard_snapshot_lifecycle.py` and `scripts/dashboard/prune_dashboard_snapshots.py` plan/apply deletion only for old `storage/06_dashboard_cache/read_models/*/snapshots/**/*.json` metadata while preserving `latest.json`, schemas, index files, Layer 1/2 data, and SQL.
- Implemented the first narrow single-file compression executor: `src/trading_storage/single_file_compression.py` and `scripts/lifecycle/compress_single_file_candidates.py` compress only unprotected `compress_candidate` regular files to zstd copies under `storage/90_lifecycle/archive/compressed/`, preserve originals, verify decompressed checksum smoke, and emit compression/restore receipts.
- Implemented the first compression/archive/restore execution scaffold: `src/trading_storage/lifecycle_execution_scaffold.py` and `scripts/lifecycle/build_lifecycle_execution_scaffold.py` consume dry-run lifecycle plans and emit non-mutating compression/archive/restore manifest and receipt drafts with `planned_not_executed` status.
- Implemented the first quarantine/recheck evidence builder: `src/trading_storage/quarantine_recheck.py` and `scripts/lifecycle/build_quarantine_recheck_evidence.py` consume a dry-run lifecycle plan plus optional final protected-set evidence, then report initial/final gate status while keeping `deletion_allowed=false` and `mutation_performed=false` for every row.
- Implemented the first durable-artifact dry-run lifecycle planner: `src/trading_storage/lifecycle_planner.py` and `scripts/lifecycle/plan_storage_lifecycle.py` combine artifact-index metadata, protected-set evidence, and reviewed policy rules into a no-mutation plan with explicit retain/compress/quarantine/archive candidate actions.
- Implemented the first protected-set builder: `src/trading_storage/protected_set.py` and `scripts/lifecycle/build_protected_set.py` consume artifact-index records plus optional reason-code refs/manual pins/candidate refs, then emit conservative safety evidence without mutating payloads or authorizing production lifecycle actions.
- Implemented the first artifact-index builder: `src/trading_storage/artifact_index.py` and `scripts/lifecycle/build_artifact_index.py` scan storage-owned filesystem artifacts/read models, emit conservative JSONL metadata, and classify ambiguous rows as manual-review/unknown-metadata protected without mutating payloads.
- Implemented the first dashboard read-model refresh wrapper: storage runs the manager-owned `historical_task_progress_summary` producer, validates/materializes the result, emits a refresh receipt, and includes systemd service/timer templates for periodic refresh without enabling host timers.
- Implemented the first dashboard read-model materialization helper: storage validates a producer-supplied common envelope, writes timestamped snapshots and `latest.json`, creates common schema placeholders, and appends index rows with checksums. Additional semantic producers/adapters remain future work.
- Accepted the first dashboard summary layout slice: storage-owned `storage/06_dashboard_cache/read_models/<contract_type>/latest.json`, timestamped snapshots, schema refs, and index JSONL are now the accepted physical boundary.
- Accepted and implemented the first local-helper slice for the durable non-SQL/runtime-staging rule: system-owned non-SQL saved data belongs in `trading-storage` by default, while semantic ownership stays with the producing component; trading runtime disposable cache/tmp/local staging belongs in storage-owned ignored roots with storage-owned scheduled cleanup; component-local staging is transitional only.
- Accepted the V0.1 storage lifecycle system design in `docs/20_storage_lifecycle_policy.md` through `docs/21_lifecycle_receipts.md`, including lifecycle states, manager-unified lifecycle requests/task visibility, artifact index, reproducibility/retention/read-mode classes, protected-set builder, quarantine-before-delete, compression/archive/restore flows, lifecycle receipts, and tombstones. Implementation remains future controlled slices and is dry-run-first.
- Closed the current storage-contract-and-lifecycle-helper phase in `docs/03_contracts.md`: V1 handoff templates, reusable checked-in non-code assets, local generated-artifact boundary, storage-owned completion receipt payload helper, and local retention/archive/cleanup helper are accepted. No production object store, SQL partitioning, provider call, manager dispatch, model activation, or broker execution is enabled by this acceptance.
- Implemented local lifecycle maintenance: `src/trading_storage/lifecycle.py`, `scripts/lifecycle/maintain_local_storage.py`, tests, and systemd timer templates under `main/templates/maintenance/`. The helper dry-runs by default, retains durable numbered roots, archives logs/runs/outputs before removal, deletes old `tmp/`, and prunes local archives after 180 days.
- Implemented the first storage slice: canonical JSON artifact writes for storage-owned completion receipt payloads under `src/trading_storage/artifact_store.py`, executable helper `scripts/artifacts/store_completion_receipt_payload.py`, and tests under `tests/`.
- Accepted package/source/test layout: `src/` for importable helpers, `scripts/` for executable entrypoints, `tests/` for first-party verification, and ignored local `storage/` for generated artifact payloads.
- Accepted V1 cross-repository handoff template contracts under `main/templates/contracts/`: `manager_request`, `run_manifest`, `artifact_ref`, and `ready_signal`.
- Clarified task-level completion receipt with nested `runs[]` entries for per-run evidence.
- Clarified that completion receipt templates should stay minimal until durable storage consumers require more fields.
- Migrated shared non-code assets from `trading-manager/storage/` to `trading-storage/main/`.
- Linked storage receipt/save planning to `trading-storage/main/templates/data_tasks/` drafts.
- Recorded that data-production development outputs may stay in local ignored staging until accepted; storage responsibility begins with durable SQL destinations and durable task completion receipts once contracts are accepted.
- Created initial `trading-storage` docs spine and repository boundary.
- Added initial `.gitignore` for local environments, generated outputs, logs, and secrets.
- Current Status infrastructure read model is implemented through `src/trading_storage/dashboard_system_status.py`, `scripts/dashboard/refresh_current_system_status_read_model.py`, and the public refresh batch `scripts/dashboard/refresh_public_dashboard_read_models.py`. The systemd refresh service now refreshes the public read-model set instead of only historical task progress.
